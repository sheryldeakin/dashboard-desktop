VITE_API_URL=http://localhost:4000

# Must match backend API_TOKEN to send `Authorization: Bearer <token>` on
# writes. NOTE: anything VITE_-prefixed is bundled into client JS and visible
# in devtools — this restricts casual/script access, it is not strong auth.
VITE_API_TOKEN=
